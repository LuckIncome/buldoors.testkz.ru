<div id="payment" class="text-left" style="display:none;"></div>
<div class="terms">

    <div class="payBtn mb-3">
        <button type="button" id="button-payment-method" class="btn btn-primary" data-loading-text="{{ text_loading }}" {% if disable_button %}{{ 'disabled' }}{% endif %}>Оплатить</button>
    </div>
    <label style="cursor: pointer; color: #cccccc; {% if not rules.display %} display:none; {% endif %}">{% if text_agree %}
            {{ text_agree }}
            <input type="checkbox" name="agree" value="1" checked="checked" />
        {% endif %}</label>
</div>